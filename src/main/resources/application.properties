# Tennis Score Computer Configuration
app.name=Tennis Score Computer
app.version=1.0.0
app.description=A clean code implementation of tennis scoring system
# Logging configuration (if needed)
logging.level.com.tennis=INFO
# Test configuration
test.performance.enabled=true
test.performance.max_duration_ms=5000